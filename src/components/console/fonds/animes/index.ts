/**
 * Fond de section animé du système Console, retenu le 24 septembre 2026.
 * Il remplit son parent (`position: absolute; inset: 0`) : le poser dans une
 * section en `position: relative`, sous le contenu.
 */

export { FondCretes } from "./Cretes";
